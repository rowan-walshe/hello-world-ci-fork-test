name: Sync fork
run-name: Sync Fork with Upstream
on:
  workflow_dispatch:
  schedule:
    - cron: "1 * * * *"
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: gh repo sync git@github.com:rowan-walshe/hello-world-ci-fork-test.git --branch master --force
